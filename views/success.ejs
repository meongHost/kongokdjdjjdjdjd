<script>
  setInterval(async () => {
    const res = await fetch('/check-payment?reffId=<%= reffId %>');
    const data = await res.json();
    if (data.status === 'success') {
      window.location.href = '/result';
    }
  }, 5000); // cek tiap 5 detik
  
</script>